---
-
  become: true
  become_method: sudo
  become_user: root
  hosts: "server1"
  tasks:
    -
      copy:
        src: /first/dummy/
        dest: /opt/tomcat/tep/webapps/
        force: yes
      name: "Remove directory"
